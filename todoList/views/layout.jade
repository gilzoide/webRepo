doctype html
html
  head
    meta(charset='utf-8')
    block title
      // Título padrão
      title TodoList
    // CSS: bootstrap + o que precisar
    link(rel="stylesheet", href="styles/bootstrap.min.css")
    block styles
    // JS: angular + o que precisar
    script(src='js/dependencies/angular.min.js')
    block scripts

  body 
    block body
